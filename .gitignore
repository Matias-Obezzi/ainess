# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# How this repo is worked on (plans, reports, scripts): local, not part of the app.
.claude/

# CLI bundle
dist-cli/

# Phone page bundle (npm run build:remote)
dist-remote/

# Per-agent transcripts the app writes as it works: local noise, not part of the project.
.ainess/history/

# What the screen tests leave behind (npm run e2e): screenshots, traces, reports. Local only.
e2e/out/
e2e/shots/
test-results/
playwright-report/
