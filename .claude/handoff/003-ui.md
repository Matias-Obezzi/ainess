# UI: layout, prompt, comunicación, jerarquía y agentes

Repo: C:\Users\matia\Desktop\projects\ais
Rama: la activa (main), sin cambiar de rama ni crear otras

## Objetivo
Que `src/App.tsx` y `src/components/*` implementen la interfaz descrita en la sección "UI" de `PLAN.md`, conectada al store real (`useAppStore`), y que `npx tsc --noEmit` y `npm run build` pasen.

## Contexto
- Leer `PLAN.md` (sección "UI" y "Store"), `src/store.ts` (API real del store; usar sus selectores), `src/types.ts`, `src/lib/providers.ts` (`PROVIDERS` para labels y `defaultModels`).
- Componentes ya instalados desde el registry `@uiness` en `src/components/ui/`: `button`, `badge`, `card`, `input`, `textarea`, `label`, `switch`, `separator`, `dialog`, `tooltip`, `select`, `tabs`, `scroll-area`, `alert`, `island`, `toast`, `progress`, `skeleton`, `avatar`, `dropdown-menu`. Leer cada uno antes de usarlo para conocer sus exports (son estilo shadcn: `Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter`, `Select, SelectTrigger, SelectValue, SelectContent, SelectItem`, `Tabs, TabsList, TabsTrigger, TabsContent`, etc.). `island.tsx` re-exporta `Island` y `island` (API `island.show/alert/confirm`), `toast.tsx` re-exporta `Toaster` y `toast`.
- Helper `cn` en `src/lib/utils.ts`. Iconos: `lucide-react`. Grafo: `@xyflow/react` (v12, importar `@xyflow/react/dist/style.css`). Selector de carpeta: `open({ directory: true, multiple: false })` de `@tauri-apps/plugin-dialog` (devuelve `string | null`).
- Tema oscuro ya activo (`<html class="dark">`); usar los tokens del tema (`bg-background`, `text-foreground`, `bg-card`, `border-border`, `text-muted-foreground`, `bg-primary`, etc.).
- `src/App.tsx` actual es el scaffold de Tauri: reemplazarlo entero. `src/main.tsx` ya importa `./index.css` y `./App`.
- tsconfig `strict` con `noUnusedLocals`/`noUnusedParameters`. Alias `@/`.

## Cambios
1. `src/App.tsx`: `useEffect(() => { void useAppStore.getState().init() }, [])`. Layout `h-screen flex flex-col bg-background text-foreground`:
   - Header (`src/components/Header.tsx`): título "AIS" + subtítulo "Orquestador de agentes"; botón con la carpeta del workspace (`config.workspaceDir ?? "Elegir workspace…"`) que abre el diálogo y llama `setWorkspaceDir`; contador de agentes trabajando; botón destructivo "Detener todo" (deshabilitado si no hay runs `running`) que llama `stopAll()`.
   - `<Island position="top" />` montado en App; el island muestra, mientras haya runs activos, una entrada compacta (`island.show({ id: "activity", leading: <Spinner/>, content: "N agentes trabajando", trailing: nombre del último agente que empezó })`) y se cierra cuando no queda ninguno. Implementarlo en un hook `useActivityIsland()` en `src/hooks/useActivityIsland.ts` que observe el store.
   - `<Toaster position="bottom-right" richColors />`. Hook `useNotifications()` en `src/hooks/useNotifications.ts` que observa `messages` y dispara `toast.info` al aparecer un `delegation` ("Claude delegó a Antigravity"), `toast.error` en `error`, `toast.success` cuando llega un `result` con `toAgentId === "user"` ("Tarea terminada"). Recordar el último id procesado para no repetir.
   - Cuerpo: `Tabs` con `Prompt`, `Comunicación`, `Jerarquía`, `Agentes`. Pestaña por defecto: Prompt.
2. `src/components/PromptPanel.tsx`: `Textarea` grande (Ctrl+Enter envía), `Select` de agente destino (opciones: todos los agentes, default = primer agente raíz con rol planner o el primer raíz), botón "Enviar" (deshabilitado si vacío o si el destino está `working`). Debajo, tarjeta de estado de la tarea actual: run raíz (`activeTaskRunId`), ronda actual, lista de agentes con su estado y `currentTask` truncado. Historial de los últimos 10 prompts (`messages` con kind `user`), clic en uno lo vuelve a cargar en el textarea. Si `binaries` del provider del destino es null mostrar `Alert` de advertencia "No se detectó el CLI de X".
3. `src/components/CommunicationPanel.tsx`: barra de filtros (`Select` de agente "Todos" + toggles por kind como `Badge` clicables: texto, herramientas, delegaciones, resultados, errores, sistema); lista dentro de `ScrollArea` con auto-scroll al final cuando llegan mensajes nuevos (si el usuario no scrolleó hacia arriba). Cada mensaje: avatar/punto con el color del agente, nombre (o "Usuario"), flecha "→ destino" si hay `toAgentId`, hora `HH:MM:SS`, `Badge` con el kind, y el texto en `whitespace-pre-wrap` monoespaciado para `tool`/`stderr`. `delegation` con borde izquierdo del color del agente destino. Botón "Limpiar" (`clearMessages`). Un componente `MessageItem.tsx` aparte.
4. `src/components/HierarchyGraph.tsx`: `ReactFlow` con `nodeTypes = { agent: AgentNode }`, `fitView`, `Background`, `Controls`, `MiniMap` opcional. Layout calculado en un `useMemo` desde `config.agents`: BFS por niveles desde las raíces (`parentId === null`), `y = nivel * 180`, `x = índice en el nivel * 260` centrado por nivel. Edges `parent -> child`, `animated: true` si `runtime[child].status === "working"`, `style.stroke` con el color del hijo. Nodos arrastrables pero no persistir posición. `AgentNode.tsx` (custom node, `Handle` top/bottom): tarjeta 220px con borde del color del agente; nombre, `Badge` con provider label y `Badge` con rol; punto de estado (idle gris, working verde pulsante `animate-pulse`, waiting amarillo, stopped naranja, error rojo) + texto del estado en español; `currentTask` truncado a 2 líneas; si el binario no está detectado, icono de advertencia con `Tooltip` "CLI no encontrado"; botones chicos: "Detener" (solo si working/waiting) → `stopAgent`, "Indicar" → abre `InstructDialog`.
5. `src/components/InstructDialog.tsx`: `Dialog` con `Textarea` y botón "Enviar" → `instructAgent(agentId, text)`; si el agente está `working` mostrar aviso "Se enviará cuando termine la tarea actual".
6. `src/components/AgentsPanel.tsx`: grid de `Card` por agente (nombre, provider, rol, padre, modelo, autoApprove, estado del binario con ruta o "No detectado", botones Editar / Eliminar / "Reiniciar sesión" → `resetSession`). Botón "Nuevo agente" y "Volver a detectar CLIs" (`detectBinaries`). `AgentDialog.tsx`: formulario para crear/editar `AgentConfig`: nombre (`Input`), provider (`Select` con `PROVIDERS`), rol (`Select`), padre (`Select` con "Ninguno (raíz)" + agentes excepto él mismo y sus descendientes), modelo (`Input` con `datalist` de `defaultModels`), `Switch` autoApprove, descripción (`Input`), instrucciones extra (`Textarea`), y si provider es `custom`: programa (`Input`) y args (`Input`, separados por espacio, con `{prompt}`). Color: `Input type="color"`. Guardar → `upsertAgent`. Eliminar pide confirmación con `island.confirm({ title: "¿Eliminar agente?", destructive: true })`.
7. `src/components/StatusDot.tsx` y `src/lib/labels.ts` (mapas `statusLabel`, `roleLabel`, `kindLabel` en español) para reutilizar.

## Casos borde y decisiones ya tomadas
- Todo el texto visible en español (rioplatense neutro: "Elegí", "Enviar", "Detener").
- Nada de librerías nuevas salvo `@xyflow/react` y `@tauri-apps/plugin-dialog`, ya instaladas.
- Si `isTauri()` es false (vite en browser) la UI debe renderizar igual con el seed; el botón de workspace muestra un `toast.error` si `open` falla.
- No modificar `src/store.ts`, `src/lib/*`, `src/types.ts` ni `src-tauri/**`. Si el store necesita algo que falta, anotarlo en el informe en vez de cambiarlo.
- `src/App.css` ya no existe; no crear CSS nuevo salvo un par de reglas en `src/index.css` si React Flow lo necesita (fondo del grafo, colores de `Controls` en dark).

## Fuera de alcance
- Persistir posiciones del grafo. Tests. Backend.

## Verificación
Desde la raíz del repo:
```
npx tsc --noEmit
npm run build
```
Ambos sin errores. Commitear con mensajes en inglés. No hacer push.
